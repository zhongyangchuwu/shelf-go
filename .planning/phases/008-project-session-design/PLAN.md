# Plan: Phase 8 Project Session Design

## Scope

Design only. Do not implement `project activate`, `project deactivate`, hooks, or `project shell` in this phase.

## Acceptance Criteria

- `CONTEXT.md` records command placement under `shelf project`.
- `CONTEXT.md` records why current-shell activation requires a shell hook/function.
- `CONTEXT.md` records reversible restore semantics for existing env vars.
- `CONTEXT.md` records `project shell` as the no-hook fallback.
- `CONTEXT.md` records repeated activation/project switching risks.

## Verification

- Planning artifact exists and maps to SES-01 through SES-04.
